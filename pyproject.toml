[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fcc"
version = "0.1.0"
description = "Forecast Combination of Time-Series Foundation Models - IJF study"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
